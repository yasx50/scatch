<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="/css/tailwind.css" rel="stylesheet">
</head>
<body class="bg-gray-50 flex items-center justify-center min-h-screen">
    <% if (messages.success) { %>
        <div class="alert alert-success bg-green-500">
            <%= messages.success %>
        </div>
    <% } %>
    <% if (messages.error) { %>
        <div class="alert alert-danger bg-rose-500">
            <%= messages.error %>
        </div>
    <% } %>

    <!-- Centered Login Form -->
    <div class="w-full max-w-md bg-gray-100 p-6 rounded-lg shadow-md">
        <h2 class="text-2xl font-semibold mb-4 text-green-600 text-center">Login</h2>
        <p class="text-gray-600 mb-4 text-center">Please login to continue to your account.</p>
        <form action="/users/login" method="POST">
            <div class="mb-4">
                <label for="email" class="block text-gray-700 font-semibold">Email</label>
                <input type="email" id="email" name="email" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-green-500" required>
            </div>
            <div class="mb-4">
                <label for="password" class="block text-gray-700 font-semibold">Password</label>
                <input type="password" id="password" name="password" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-green-500" required>
            </div>
            <button type="submit" class="w-full bg-green-600 text-white py-2 rounded-lg font-semibold hover:bg-green-700 transition">Login</button>
        </form>
        <p class="text-gray-600 mt-4 text-center">Forgot your password? <a href="#" class="text-green-600 hover:underline">Reset it here</a>.</p>
    </div>
</body>
</html>
