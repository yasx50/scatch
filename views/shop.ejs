<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Listing - Scatch</title>
  <link href="/css/tailwind.css" rel="stylesheet">
</head>
<body class="bg-slate-600">
  <!-- Success and Error Messages -->
  <% if (messages.success) { %>
    <div class="alert alert-success bg-green-500 text-white p-4 rounded-lg mb-4 text-center">
      <%= messages.success %>
    </div>
  <% } %>
  <% if (messages.error) { %>
    <div class="alert alert-danger bg-rose-500 text-white p-4 rounded-lg mb-4 text-center">
      <%= messages.error %>
    </div>
  <% } %>


  <!-- Navbar Section for Desktop Only -->
  <nav class=" text-white py-4 shadow-md">
    <div class="container mx-auto flex justify-between items-center">
      <!-- Brand Name -->
      <a href="/" class="text-2xl font-semibold text-purple-900 ">Scatch</a>
  
      <!-- Navbar Links for Desktop -->
      <ul class="flex space-x-8">
        <li><a href="/" class="hover:text-gray-400">Home</a></li>
        <li><a href="/shop" class="hover:text-gray-400">Shop</a></li>
        <li><a href="/cart" class="hover:text-gray-400">My Cart</a></li>
        <li><a href="/account" class="hover:text-gray-400">My Account</a></li>
        <li><a href="/users/logout" class="hover:text-gray-400">Logout</a></li>
      </ul>
    </div>
  </nav>
  

  
  <!-- Product Listing Section -->
  <div class="container mx-auto mt-10">

    <!-- Product Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
     
      <% products.forEach(product => { %>

      <!-- Product Card -->
      <div style="background-color: <%= product.bgcolor %>" class="shadow-lg rounded-lg overflow-hidden transform hover:scale-105 transition-transform duration-300">
        <img src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="Product Image" class="w-full h-48 object-cover">
        <div class="p-6">
          
          <!-- Product Name with Dynamic Text Color -->
          <h3 style=" background-color: <%= product.textcolor %>" class="text-xl font-semibold p-2 text-center rounded"><%= product.name %></h3>
          
          <!-- New Product Badge or Discount -->
          <% if (product.new) { %>
            <span class="bg-green-500 text-white text-xs px-2 py-1 rounded-full inline-block mt-2">New Arrival</span>
          <% } %>
          <% if (product.discount) { %>
            <span class="bg-red-500 text-white text-xs px-2 py-1 rounded-full inline-block mt-2">Discount: <%= product.discount %>% off</span>
          <% } %>

          <!-- Product Price -->
          <p class="text-gray-600 mt-4">Price: ₹<%= product.price %></p>

          <!-- Product Short Description -->
          <p class="text-sm text-gray-500 mt-2">Get the best of <%= product.name %> at an unbeatable price. Limited stock available!</p>

          <!-- Add to Cart Button -->
          <a href="/shop" class="mt-4 inline-block bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition">Add to Cart</a>
        </div>
      </div>

      <% }) %>
    </div>

    <!-- End Product Grid -->
    
    <!-- Additional Info Section -->
    <div class="mt-16 bg-gray-200 p-8 rounded-lg text-center">
      <h3 class="text-xl font-semibold mb-4 text-gray-800">Why Shop With Us?</h3>
      <p class="text-gray-600 mb-4">We offer a wide range of high-quality products that cater to your needs. From the latest arrivals to unbeatable deals, you'll find it all here. Experience seamless shopping with our easy-to-use interface and reliable delivery service.</p>
      <p class="text-gray-600">Need help? Our customer support is available 24/7. Shop now and enjoy exclusive offers!</p>
    </div>
  </div>

</body>
</html>
